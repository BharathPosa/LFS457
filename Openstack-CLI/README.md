## README

```
# Copy following files in $HOME (trainee home directory)
- sample_template.yaml
- clouds.yaml

# Create following directory
mkdir $HOME/.config
mkdir $HOME/.config/openstack

# Copy clouds.yaml
cp $HOME/clouds.yaml $HOME/.config/openstack/clouds.yaml

# Follow the instructions in lab exercise to use sample_template.yaml
```
